:root {
  --bg: #f3efe7;
  --surface: #fffdf8;
  --surface-2: #f8f4ec;
  --text: #253129;
  --muted: #6f776f;
  --line: #ddd7ca;
  --green: #416a4b;
  --green-soft: #dce9dc;
  --yellow: #b48124;
  --yellow-soft: #f4e8c6;
  --gray: #7b817d;
  --gray-soft: #e7e9e6;
  --red: #a74d3f;
  --red-soft: #f1d9d3;
  --shadow: 0 16px 50px rgba(58, 50, 37, 0.10);
  --radius: 20px;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  color: var(--text);
  background:
    radial-gradient(circle at 70% 0%, rgba(210, 179, 128, 0.18), transparent 34%),
    var(--bg);
}

button, input, select, textarea { font: inherit; }
button, a { -webkit-tap-highlight-color: transparent; }

.app-shell, .analysis-shell {
  width: min(1540px, calc(100% - 48px));
  margin: 0 auto;
  padding: 28px 0 40px;
}

.topbar, .analysis-header {
  display: flex;
  justify-content: space-between;
  gap: 28px;
  align-items: flex-start;
  padding: 10px 2px 22px;
}

.eyebrow {
  display: inline-block;
  color: var(--green);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: .16em;
  margin-bottom: 8px;
}

h1, h2, h3, p { margin-top: 0; }
h1 { font-size: clamp(30px, 4vw, 48px); letter-spacing: -0.04em; margin-bottom: 8px; }
.topbar p, .analysis-header p { color: var(--muted); font-size: 18px; margin-bottom: 0; }

.top-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  justify-content: flex-end;
  max-width: 620px;
}

.btn {
  min-height: 44px;
  border-radius: 12px;
  padding: 0 16px;
  border: 1px solid transparent;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-weight: 750;
}
.btn.primary { background: var(--green); color: white; }
.btn.secondary { background: var(--green-soft); color: #254b30; }
.btn.ghost { background: var(--surface); border-color: var(--line); color: var(--text); }
.btn.danger { background: var(--red-soft); color: #7e352b; }

.indicators {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 12px;
  margin-bottom: 18px;
}
.indicators article {
  background: rgba(255,253,248,.86);
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 16px 18px;
  display: flex;
  align-items: baseline;
  justify-content: space-between;
}
.indicators span { color: var(--muted); font-size: 14px; }
.indicators strong { font-size: 28px; }

.layout {
  display: grid;
  grid-template-columns: 250px minmax(0, 1fr);
  gap: 18px;
  align-items: start;
}

.sidebar, .radar-card, .list-card, .analysis-card, .trace-card, .decision-card {
  background: rgba(255,253,248,.94);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

.sidebar {
  padding: 18px;
  position: sticky;
  top: 16px;
}
.sidebar-title {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.sidebar h2 { font-size: 20px; margin-bottom: 0; }
.text-button, .link-button {
  background: none;
  border: 0;
  color: var(--green);
  cursor: pointer;
  font-weight: 750;
  padding: 4px;
}
.sidebar fieldset {
  border: 0;
  padding: 0;
  margin: 18px 0;
}
.sidebar legend, .sidebar label > span {
  font-size: 13px;
  color: var(--muted);
  font-weight: 750;
  margin-bottom: 7px;
  display: block;
}
.radio-row {
  display: flex;
  gap: 9px;
  align-items: center;
  padding: 8px 0;
}
.sidebar select, .form-grid input, .form-grid textarea {
  width: 100%;
  border: 1px solid var(--line);
  background: white;
  border-radius: 10px;
  padding: 10px 12px;
  margin-bottom: 13px;
}

.radar-card, .list-card { padding: 20px; }
.list-card { margin-top: 18px; }

.section-heading {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: start;
  margin-bottom: 16px;
}
.section-heading h2 { margin-bottom: 4px; font-size: 22px; }
.section-heading p { margin-bottom: 0; color: var(--muted); }

.legend { display: flex; gap: 14px; flex-wrap: wrap; font-size: 13px; color: var(--muted); }
.legend span { display: inline-flex; align-items: center; gap: 6px; }
.dot { width: 10px; height: 10px; border-radius: 50%; display: inline-block; }
.dot.high { background: var(--green); }
.dot.review { background: var(--yellow); }
.dot.low { background: var(--gray); }
.dot.noise { background: var(--red); }

.radar-stage {
  position: relative;
  width: 100%;
  aspect-ratio: 16 / 8.5;
  min-height: 540px;
  overflow: hidden;
  border-radius: 18px;
  background:
    linear-gradient(rgba(65,106,75,.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(65,106,75,.035) 1px, transparent 1px),
    radial-gradient(circle, rgba(255,255,255,.95), rgba(220,233,220,.42));
  background-size: 32px 32px, 32px 32px, auto;
  border: 1px solid #d5ddcf;
}

.radar-ring {
  position: absolute;
  left: 50%; top: 50%;
  transform: translate(-50%, -50%);
  border: 1px solid rgba(65,106,75,.25);
  border-radius: 50%;
}
.ring-1 { width: 18%; aspect-ratio: 1; }
.ring-2 { width: 36%; aspect-ratio: 1; }
.ring-3 { width: 56%; aspect-ratio: 1; }
.ring-4 { width: 78%; aspect-ratio: 1; }

.radar-axis { position: absolute; background: rgba(65,106,75,.12); }
.radar-axis.vertical { width: 1px; height: 86%; left: 50%; top: 7%; }
.radar-axis.horizontal { height: 1px; width: 86%; top: 50%; left: 7%; }

.radar-center {
  position: absolute;
  left: 50%; top: 50%;
  transform: translate(-50%, -50%);
  width: 82px; height: 82px;
  border-radius: 50%;
  background: #30563a;
  color: white;
  display: grid;
  place-items: center;
  font-size: 11px;
  font-weight: 850;
  letter-spacing: .08em;
  z-index: 3;
  box-shadow: 0 10px 28px rgba(48,86,58,.25);
}

.bubble {
  position: absolute;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,.8);
  padding: 13px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  text-align: center;
  cursor: pointer;
  box-shadow: 0 10px 24px rgba(50,45,36,.16);
  transition: transform .16s ease, box-shadow .16s ease;
  z-index: 4;
}
.bubble:hover, .bubble:focus-visible {
  transform: translate(-50%, -50%) scale(1.06);
  box-shadow: 0 14px 34px rgba(50,45,36,.24);
  z-index: 8;
}
.bubble strong {
  font-size: clamp(10px, 1vw, 13px);
  line-height: 1.2;
}
.bubble small {
  font-size: 10px;
  margin-top: 6px;
  opacity: .78;
}
.bubble-alta { background: var(--green-soft); color: #21442b; }
.bubble-revisar { background: var(--yellow-soft); color: #694914; }
.bubble-baja { background: var(--gray-soft); color: #4d534f; }
.bubble-ruido { background: var(--red-soft); color: #74362d; }

.table-wrap { overflow-x: auto; }
table { width: 100%; border-collapse: collapse; min-width: 760px; }
th, td { padding: 13px 10px; text-align: left; border-top: 1px solid var(--line); }
th { color: var(--muted); font-size: 12px; text-transform: uppercase; letter-spacing: .04em; }
td { font-size: 14px; }

.status-pill {
  display: inline-flex;
  border-radius: 999px;
  padding: 6px 10px;
  font-size: 12px;
  font-weight: 800;
}
.status-pill.alta { background: var(--green-soft); color: #254b30; }
.status-pill.revisar { background: var(--yellow-soft); color: #6d4a10; }
.status-pill.baja { background: var(--gray-soft); color: #555b57; }
.status-pill.ruido { background: var(--red-soft); color: #78362d; }

.human-rule {
  display: flex;
  justify-content: center;
  gap: 8px;
  margin-top: 18px;
  padding: 16px;
  color: var(--muted);
}
.human-rule strong { color: var(--green); }

.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(27,35,29,.58);
  z-index: 30;
  display: grid;
  place-items: center;
  padding: 24px;
}
.modal {
  width: min(820px, 100%);
  max-height: 90vh;
  overflow: auto;
  background: var(--surface);
  border-radius: 22px;
  box-shadow: 0 30px 90px rgba(0,0,0,.30);
}
.modal-header, .modal-actions {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  padding: 20px 22px;
}
.modal-header { border-bottom: 1px solid var(--line); }
.modal-header h2 { margin-bottom: 0; }
.modal-body { padding: 22px; }
.modal-actions { border-top: 1px solid var(--line); flex-wrap: wrap; justify-content: flex-end; }
.icon-button {
  width: 38px; height: 38px; border-radius: 50%;
  border: 1px solid var(--line); background: white; cursor: pointer; font-size: 24px;
}
.detail-grid, .trace-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}
.detail-grid div, .trace-grid div {
  background: var(--surface-2);
  border-radius: 12px;
  padding: 13px;
}
.detail-grid span, .trace-grid span {
  display: block;
  color: var(--muted);
  font-size: 12px;
  margin-bottom: 4px;
}
.conversation-text, .reason-box {
  margin-top: 18px;
  padding: 18px;
  border-radius: 15px;
}
.conversation-text { background: #f0eee7; }
.reason-box { background: var(--green-soft); }
.conversation-text h3, .reason-box h3 { font-size: 15px; margin-bottom: 8px; }
.conversation-text p, .reason-box p { margin-bottom: 0; line-height: 1.65; }

.back-link {
  display: inline-block;
  margin-bottom: 22px;
  color: var(--green);
  text-decoration: none;
  font-weight: 750;
}
.analysis-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px;
}
.analysis-card, .trace-card, .decision-card { padding: 24px; }
.analysis-card h2, .trace-card h2, .decision-card h2 { font-size: 22px; }
.conversation-text.large p { font-size: 18px; }

.meta-list, .analysis-list { margin: 20px 0 0; }
.meta-list div, .analysis-list div {
  display: grid;
  grid-template-columns: 170px 1fr;
  gap: 15px;
  padding: 12px 0;
  border-top: 1px solid var(--line);
}
dt { color: var(--muted); }
dd { margin: 0; font-weight: 700; }
.analysis-section {
  border-top: 1px solid var(--line);
  padding-top: 16px;
  margin-top: 16px;
}
.analysis-section h3 { font-size: 15px; }
.analysis-section li { margin-bottom: 7px; }
.trace-card, .decision-card { margin-top: 18px; }
.trace-grid { grid-template-columns: repeat(5, 1fr); }

.decision-options {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin: 20px 0;
}
.decision-options label {
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 11px 13px;
  background: white;
}
.form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}
.form-grid .full { grid-column: 1 / -1; }
.form-grid label > span { display: block; color: var(--muted); font-size: 13px; font-weight: 750; margin-bottom: 6px; }
.decision-footer { display: flex; justify-content: space-between; margin-top: 8px; }
.decision-card.success { display: flex; align-items: center; gap: 18px; }
.success-mark {
  width: 58px; height: 58px; border-radius: 50%;
  display: grid; place-items: center;
  background: var(--green-soft); color: var(--green); font-size: 28px; font-weight: 900;
}
.empty-state {
  background: var(--surface);
  border: 1px dashed var(--line);
  border-radius: var(--radius);
  padding: 70px 30px;
  text-align: center;
}
.empty-icon { font-size: 54px; color: var(--green); }

@media (max-width: 1100px) {
  .topbar, .analysis-header { flex-direction: column; }
  .top-actions { justify-content: flex-start; max-width: none; }
  .indicators { grid-template-columns: repeat(3, 1fr); }
  .layout { grid-template-columns: 220px minmax(0, 1fr); }
  .radar-stage { min-height: 500px; aspect-ratio: auto; }
  .trace-grid { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 820px) {
  .app-shell, .analysis-shell { width: min(100% - 24px, 100%); }
  .layout, .analysis-grid { grid-template-columns: 1fr; }
  .sidebar { position: static; }
  .indicators { grid-template-columns: repeat(2, 1fr); }
  .radar-stage { min-height: 650px; }
  .detail-grid, .trace-grid, .form-grid { grid-template-columns: 1fr; }
  .form-grid .full { grid-column: auto; }
  .meta-list div, .analysis-list div { grid-template-columns: 1fr; gap: 4px; }
}

.human-rule{display:flex;gap:6px;justify-content:center;margin-top:22px;color:var(--muted)}
.bubble-alta{background:var(--green-soft);color:#254b30}.bubble-media,.bubble-sin-evaluacion{background:var(--yellow-soft);color:#765515}.bubble-baja{background:var(--red-soft);color:#7e352b}
.status-pill.alta{background:var(--green-soft);color:#254b30}.status-pill.media,.status-pill.sin-evaluacion{background:var(--yellow-soft);color:#765515}.status-pill.baja{background:var(--red-soft);color:#7e352b}
.compact-trace{margin-top:16px}.decision-card.warning{border-color:#d6a23b}button:disabled{opacity:.58;cursor:not-allowed}
@media(max-width:980px){.layout{grid-template-columns:1fr}.sidebar{position:static}.indicators{grid-template-columns:repeat(2,1fr)}}

/* Nuevo mapa visual por carriles: reemplaza el radar circular caótico */
.signal-map-card {
  background: rgba(255,253,248,.94);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 20px;
}
.signal-board {
  display: grid;
  grid-template-columns: repeat(4, minmax(210px, 1fr));
  gap: 14px;
  align-items: stretch;
}
.signal-lane {
  min-height: 460px;
  border: 1px solid var(--line);
  border-radius: 18px;
  background: var(--surface-2);
  padding: 14px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.signal-lane header {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  align-items: flex-start;
  padding-bottom: 10px;
  border-bottom: 1px solid rgba(65,106,75,.14);
}
.signal-lane h3 { margin: 0 0 4px; font-size: 17px; }
.signal-lane p { margin: 0; color: var(--muted); font-size: 13px; line-height: 1.35; }
.signal-lane header strong {
  min-width: 34px;
  height: 34px;
  border-radius: 999px;
  display: grid;
  place-items: center;
  background: white;
  color: var(--green);
  border: 1px solid var(--line);
}
.signal-items {
  display: grid;
  gap: 10px;
  align-content: start;
  max-height: 640px;
  overflow: auto;
  padding-right: 4px;
}
.signal-card {
  width: 100%;
  text-align: left;
  border: 1px solid rgba(65,106,75,.16);
  border-radius: 14px;
  background: white;
  padding: 12px;
  cursor: pointer;
  display: grid;
  gap: 7px;
  color: var(--text);
  box-shadow: 0 8px 20px rgba(58, 50, 37, .06);
}
.signal-card:hover,
.signal-card:focus-visible {
  border-color: var(--green);
  box-shadow: 0 12px 26px rgba(58, 50, 37, .12);
  transform: translateY(-1px);
}
.signal-card strong { font-size: 14px; line-height: 1.25; }
.signal-card small { color: var(--muted); font-size: 12px; }
.signal-card em { color: var(--muted); font-size: 12px; font-style: normal; }
.source-chip {
  justify-self: start;
  border-radius: 999px;
  background: var(--green-soft);
  color: #254b30;
  padding: 3px 8px;
  font-size: 11px;
  font-weight: 800;
}
.lane-alta { background: linear-gradient(180deg, rgba(220,233,220,.75), rgba(255,253,248,.95)); }
.lane-media, .lane-sin-evaluacion { background: linear-gradient(180deg, rgba(244,232,198,.75), rgba(255,253,248,.95)); }
.lane-baja { background: linear-gradient(180deg, rgba(241,217,211,.65), rgba(255,253,248,.95)); }
@media (max-width: 1240px) { .signal-board { grid-template-columns: repeat(2, minmax(220px, 1fr)); } }
@media (max-width: 760px) { .signal-board { grid-template-columns: 1fr; } .signal-lane { min-height: auto; } }
