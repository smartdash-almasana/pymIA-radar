:root {
  --page: #f3f0e9;
  --paper: #fffdfa;
  --ink: #233229;
  --muted: #6c746f;
  --line: #d8d5cb;
  --green: #2f5b49;
  --green-soft: #e8f0eb;
  --sand: #f4ead5;
  --sand-ink: #72551d;
  --rose: #f4e6e3;
  --rose-ink: #7b4038;
  --shadow: 0 18px 45px rgba(31, 49, 40, .09);
}

* { box-sizing: border-box; }

body {
  margin: 0;
  min-height: 100vh;
  background: var(--page);
  color: var(--ink);
  font: 17px/1.6 "Segoe UI", system-ui, sans-serif;
}

button, input { font: inherit; }
button { color: inherit; }
.hidden { display: none !important; }

.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
  padding: 22px clamp(18px, 5vw, 64px);
  background: var(--paper);
  border-bottom: 1px solid var(--line);
}

.brand {
  margin: 0 0 2px;
  color: var(--green);
  font-size: .72rem;
  font-weight: 800;
  letter-spacing: .16em;
}

.topbar h1 {
  margin: 0;
  font-size: clamp(1.2rem, 2vw, 1.55rem);
  line-height: 1.2;
}

.topbar-actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.status-badge {
  padding: 7px 11px;
  border-radius: 999px;
  background: var(--green-soft);
  color: var(--green);
  font-size: .8rem;
  font-weight: 750;
}

.status-badge.offline {
  background: var(--rose);
  color: var(--rose-ink);
}

.small-button, .nav-button, .confirm-button {
  border: 1px solid var(--line);
  border-radius: 10px;
  padding: 9px 13px;
  background: var(--paper);
  cursor: pointer;
  font-weight: 700;
}

.small-button:hover, .nav-button:hover { border-color: var(--green); }
.confirm-button { background: var(--green); color: white; border-color: var(--green); }
button:disabled { opacity: .45; cursor: not-allowed; }

.review-shell {
  width: min(920px, calc(100% - 28px));
  margin: 0 auto;
  padding: 28px 0 56px;
}

.conversation-view {
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.conversation-navigation {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  gap: 14px;
}

.conversation-navigation .nav-button:last-child { justify-self: end; }
.progress-text { margin: 0; color: var(--muted); font-size: .9rem; font-weight: 700; }

.conversation-card {
  padding: clamp(24px, 5vw, 54px);
  background: var(--paper);
  border: 1px solid var(--line);
  border-radius: 22px;
  box-shadow: var(--shadow);
}

.conversation-origin {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  margin-bottom: 30px;
}

.source-text {
  margin: 0;
  color: var(--green);
  font-weight: 800;
}

.author-date {
  margin: 3px 0 0;
  color: var(--muted);
  font-size: .92rem;
}

.review-state {
  flex: 0 0 auto;
  padding: 7px 11px;
  border-radius: 999px;
  background: #efeee8;
  color: #56615b;
  font-size: .78rem;
  font-weight: 750;
}

.conversation-title {
  margin: 0 0 20px;
  font-size: clamp(1.6rem, 4vw, 2.45rem);
  line-height: 1.18;
}

.conversation-text {
  margin: 0;
  white-space: pre-wrap;
  font-size: clamp(1.05rem, 2vw, 1.2rem);
  line-height: 1.75;
}

.context-block {
  margin-top: 28px;
  padding: 18px 20px;
  border-left: 4px solid var(--green);
  border-radius: 0 12px 12px 0;
  background: #f5f4ef;
}

.context-block p { margin: 0; }
.context-label { margin-bottom: 5px !important; color: var(--green); font-size: .8rem; font-weight: 800; text-transform: uppercase; letter-spacing: .08em; }

.original-link {
  display: inline-flex;
  margin-top: 30px;
  color: var(--green);
  font-weight: 800;
  text-decoration: none;
}

.original-link:hover { text-decoration: underline; }

.decision-section {
  padding: 28px;
  background: var(--paper);
  border: 1px solid var(--line);
  border-radius: 18px;
}

.decision-section h2 {
  margin: 0 0 18px;
  text-align: center;
  font-size: 1.35rem;
}

.decision-buttons {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.decision-button {
  min-height: 128px;
  padding: 18px;
  border: 1px solid transparent;
  border-radius: 14px;
  cursor: pointer;
  text-align: left;
  transition: transform .15s ease, box-shadow .15s ease;
}

.decision-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 24px rgba(35, 50, 41, .1);
}

.decision-button strong,
.decision-button span { display: block; }
.decision-button strong { margin-bottom: 6px; font-size: 1rem; }
.decision-button span { font-size: .86rem; line-height: 1.45; opacity: .82; }
.decision-button.discard { background: var(--rose); color: var(--rose-ink); }
.decision-button.later { background: var(--sand); color: var(--sand-ink); }
.decision-button.approach { background: var(--green); color: white; }

.reviewed-notice, .completion-notice {
  padding: 18px 22px;
  border-radius: 14px;
  text-align: center;
}

.reviewed-notice { background: #ebeae4; color: #59625d; }
.reviewed-notice p { margin: 0; }
.completion-notice { display: flex; flex-direction: column; gap: 3px; background: var(--green-soft); color: var(--green); }

.empty-state {
  padding: 90px 24px;
  text-align: center;
  background: var(--paper);
  border: 1px solid var(--line);
  border-radius: 22px;
}

.empty-state h2 { margin: 8px 0; }
.empty-state p { margin: 0; color: var(--muted); }
.empty-mark { margin: 0 !important; color: var(--green) !important; font-size: 2rem; font-weight: 900; }

.reviewer-dialog {
  width: min(440px, calc(100% - 28px));
  padding: 0;
  border: 0;
  border-radius: 18px;
  box-shadow: var(--shadow);
}

.reviewer-dialog::backdrop { background: rgba(25, 35, 30, .48); }
.reviewer-dialog form { padding: 28px; }
.reviewer-dialog h2 { margin: 0 0 6px; }
.reviewer-dialog p { margin: 0 0 20px; color: var(--muted); }
.reviewer-dialog label { display: block; margin-bottom: 6px; font-weight: 750; }
.reviewer-dialog input { width: 100%; padding: 11px 12px; border: 1px solid var(--line); border-radius: 10px; }
.dialog-actions { display: flex; justify-content: flex-end; gap: 10px; margin-top: 20px; }

.toast {
  position: fixed;
  right: 22px;
  bottom: 22px;
  z-index: 20;
  max-width: min(420px, calc(100% - 44px));
  padding: 13px 16px;
  border-radius: 11px;
  background: var(--ink);
  color: white;
  box-shadow: var(--shadow);
}

.toast.error { background: #873f38; }

@media (max-width: 760px) {
  .topbar { align-items: flex-start; }
  .topbar-actions { flex-direction: column; align-items: flex-end; }
  .conversation-navigation { grid-template-columns: 1fr 1fr; }
  .progress-text { grid-column: 1 / -1; grid-row: 1; text-align: center; }
  .conversation-navigation .nav-button { grid-row: 2; }
  .decision-buttons { grid-template-columns: 1fr; }
  .decision-button { min-height: 0; }
  .conversation-origin { flex-direction: column; }
}

@media (max-width: 480px) {
  body { font-size: 16px; }
  .topbar { padding: 16px; }
  .status-badge { display: none; }
  .review-shell { width: min(100% - 18px, 920px); padding-top: 12px; }
  .conversation-card { padding: 22px 18px; border-radius: 16px; }
  .decision-section { padding: 18px; }
  .nav-button { padding: 9px 10px; }
}
