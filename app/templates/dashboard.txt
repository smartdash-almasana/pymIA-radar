<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>RADAR · Inlak’ech</title>
  <link rel="stylesheet" href="/static/radar.css">
</head>
<body>
<header class="topbar">
  <div>
    <p class="brand">INLAK’ECH</p>
    <h1>Conversaciones encontradas</h1>
  </div>
  <div class="topbar-actions">
    <span id="healthBadge" class="status-badge">Conectando…</span>
    <button id="refreshButton" class="small-button" type="button">Actualizar</button>
  </div>
</header>

<main class="review-shell">
  <section id="emptyState" class="empty-state hidden" aria-live="polite">
    <p class="empty-mark">✓</p>
    <h2>Todavía no hay conversaciones para revisar</h2>
    <p>Cuando RADAR encuentre nuevas conversaciones, van a aparecer acá.</p>
  </section>

  <article id="conversationView" class="conversation-view hidden">
    <nav class="conversation-navigation" aria-label="Navegación entre conversaciones">
      <button id="previousButton" class="nav-button" type="button">← Anterior</button>
      <p id="progressText" class="progress-text">Conversación 1 de 1</p>
      <button id="nextButton" class="nav-button" type="button">Siguiente →</button>
    </nav>

    <section class="conversation-card">
      <div class="conversation-origin">
        <div>
          <p id="sourceText" class="source-text">Encontrada en…</p>
          <p id="authorDateText" class="author-date"></p>
        </div>
        <span id="reviewState" class="review-state">Sin revisar</span>
      </div>

      <h2 id="conversationTitle" class="conversation-title"></h2>
      <p id="conversationText" class="conversation-text"></p>

      <div id="contextBlock" class="context-block hidden">
        <p class="context-label">Contexto</p>
        <p id="conversationContext"></p>
      </div>

      <a id="originalLink" class="original-link" target="_blank" rel="noopener noreferrer">
        Ver conversación original ↗
      </a>
    </section>

    <section id="decisionSection" class="decision-section">
      <h2>¿Qué hacemos con esta conversación?</h2>
      <div class="decision-buttons">
        <button id="discardButton" class="decision-button discard" type="button">
          <strong>No corresponde</strong>
          <span>No tiene relación suficiente con Inlak’ech.</span>
        </button>
        <button id="laterButton" class="decision-button later" type="button">
          <strong>Dejar para más adelante</strong>
          <span>Todavía no hay elementos suficientes.</span>
        </button>
        <button id="approachButton" class="decision-button approach" type="button">
          <strong>Vale la pena acercarnos</strong>
          <span>Merece una mirada humana más cercana.</span>
        </button>
      </div>
    </section>

    <section id="reviewedNotice" class="reviewed-notice hidden" aria-live="polite">
      <p id="reviewedText"></p>
    </section>

    <section id="completionNotice" class="completion-notice hidden" aria-live="polite">
      <strong>Ya revisaste todas las conversaciones disponibles.</strong>
      <span>Podés volver con los botones Anterior y Siguiente.</span>
    </section>
  </article>
</main>

<dialog id="reviewerDialog" class="reviewer-dialog">
  <form id="reviewerForm" method="dialog">
    <h2>¿Quién está revisando?</h2>
    <p>Lo pedimos una sola vez para dejar constancia de la decisión.</p>
    <label for="reviewerName">Nombre</label>
    <input id="reviewerName" autocomplete="name" required maxlength="255">
    <div class="dialog-actions">
      <button id="cancelReviewerButton" class="small-button" type="button">Cancelar</button>
      <button class="confirm-button" type="submit">Continuar</button>
    </div>
  </form>
</dialog>

<div id="toast" class="toast hidden" role="status" aria-live="polite"></div>
<script src="/static/radar.js" defer></script>
</body>
</html>
